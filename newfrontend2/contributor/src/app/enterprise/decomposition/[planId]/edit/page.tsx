import { redirect } from "next/navigation";

/** Phase 1B continuity hardening — V1 surface folded into V2 ecosystem. */
export default function V1Redirect(): never {
  redirect("/enterprise/decomposition");
}
